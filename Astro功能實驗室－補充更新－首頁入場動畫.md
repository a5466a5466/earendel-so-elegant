# Astro 功能實驗室・補充更新・首頁入場動畫

- 建立日期：2026-07-23
- 目前狀態：第一版已實作，待使用者人工視覺驗收
- 類型：Phase 1～4 結案後的補充更新
- 目標頁面：正式首頁 `/`
- 參考網站：`https://cyg.sid.tw/`
- Dependency：預計使用原生 CSS 與少量 TypeScript，不新增套件

## 1. 文件目的

本文件管理正式首頁的入場動畫補充需求。此工作不回寫或重開既有 Phase 1～4，也不納入已結案 Step；後續設計、實作、QA、人工驗收與部署紀錄都集中追加於本文件。

## 2. 參考效果觀察

2026-07-23 已實際重新載入參考網站並分段觀察：

1. 畫面先以全黑背景開始。
2. 頂部導覽逐漸淡入。
3. 中央顯示大型白色主標文字。
4. 右下角小角色與主標在相近階段進場。
5. 約一至兩秒後主標退場，首頁主要內容接續揭露。

本站只參考「全螢幕、分階段揭露、主標退場後進入首頁」的敘事節奏，不複製對方文字、圖片、角色、品牌識別或程式碼。

## 3. 本站候選版本

預定流程：

```text
深色全螢幕
→ 星光或厄倫蒂兒識別元素出現
→ 厄倫蒂兒主標題進場
→ 主標題退場
→ 正式首頁 Hero 與導覽揭露
```

視覺方向沿用本站既有深藍、金色、星光與典雅字體，不建立與參考網站相同的黑白視覺複本。正式文案、識別素材與時間曲線需在實作前由使用者確認。

## 4. 第一版範圍

- 只處理正式首頁首次載入的視覺揭露。
- 使用原生 CSS animation／transition 與少量 TypeScript 控制完成狀態。
- 首頁主要 HTML 必須預先存在，不等待動畫才建立內容。
- 動畫層不得阻擋頁面載入失敗時的閱讀與導覽。
- 動畫完成後必須完整移除覆蓋層與互動攔截。
- 暫不加入影片、音訊、Canvas、WebGL、Live2D 或新 dependency。

## 5. 待決策項目

- 入場動畫每次重新整理都播放，或只在同一瀏覽工作階段首次進站播放。
- 主標文案與是否加入副標。
- 使用純文字、現有星光圖形，或另製正式識別素材。
- 總長度、各階段節奏與略過方式。
- 手機版是否縮短時間或減少元素，但不得自行切換成低效能模式。

## 6. 必要行為與防護

- 使用者選擇「減少動態」時，改為短淡入或直接顯示首頁。
- 提供可被鍵盤與指標操作的「略過動畫」方式，或允許任意互動立即完成；最終形式於實作時確認。
- 動畫期間不得鎖死返回、重新整理、網址列或瀏覽器操作。
- JavaScript 關閉、CSS 載入失敗或 runtime 例外時，首頁內容仍直接可見。
- 避免造成 layout shift；覆蓋層使用固定定位，首頁本體維持正常版面。
- 不自動播放聲音；若未來加入聲音，必須另開需求並遵守使用者手勢限制。

## 7. 實作順序

1. 確認主標文案、識別素材、是否每次播放與總長度。
2. 建立純 CSS／HTML 靜態原型，確認首尾畫面。
3. 加入階段控制、略過與完成後清理。
4. 驗證桌機與手機版，不影響首頁內容、導覽與既有 Lab 入口。
5. 執行 Lab／production build、瀏覽器 Console 與動畫時間 QA。
6. 使用者人工確認節奏與視覺後，更新本文件、`handoff.md`、commit 並 push。

## 8. 驗收條件

- 入場流程清楚但不拖延進站，總時間符合使用者核准值。
- 主標、首頁 Hero 與導覽的出場／進場順序穩定。
- 動畫完成或略過後，不殘留透明遮罩、焦點陷阱、scroll lock 或 listener。
- 360px、768px、1440px 無溢位或文字裁切。
- 重新整理、返回首頁、深層頁返回與 JavaScript 關閉皆可正常使用。
- Console error／warning 為 0，建置與 GitHub Pages Repository base 路徑通過。
- 使用者完成人工驗收後才標記完成。

## 9. 第一版實作紀錄（2026-07-23）

第一版依下列預設完成：

- 播放頻率：目前每次載入首頁皆播放；不使用儲存狀態，優先確保所有預覽環境穩定顯示。
- 主標文案：「厄倫蒂兒／好氣質，從星光開始」，並沿用首頁既有英文 kicker。
- 素材：只使用 CSS 星點、軌道與既有星芒 SVG 語彙，不新增圖片或 dependency。
- 時間：2.8 秒後開始以 0.5 秒淡出；覆蓋層保留於 DOM 但轉為 `visibility: hidden` 且不攔截互動。
- 略過：右下角提供可由指標或鍵盤操作的「略過動畫」控制。
- 動態政策：依專案目前「預設完整動畫」決策固定播放，不因系統 `prefers-reduced-motion` 自動跳過。
- 失敗策略：入場流程完全由 HTML／CSS 驅動，不依賴 JavaScript 初始化；首頁內容始終預先存在。

`index.astro` 負責覆蓋層 markup、動畫樣式與 CSS-only 略過控制。最初使用根節點 dataset 與 `sessionStorage` 的版本在內建瀏覽器導覽期間會過早操作尚未掛載的 DOM，造成首頁空白，因此已移除該初始化路徑。

## 10. 技術驗證與待驗收

- `pnpm build:lab` 通過 39 頁，Astro 與 TypeScript 編譯沒有錯誤。
- 首頁靜態輸出包含 title、完整主要內容與動畫覆蓋層，不需要入場判定 script。
- 內建瀏覽器已確認首頁 DOM 與標題正常、Console error 為 0；動畫開始時覆蓋層可見，3.4 秒後為 hidden／opacity 0，主內容保持可見。
- 「略過首頁入場動畫」控制可被定位並切換；切換後覆蓋層立即 hidden，主內容保持可見。
- Hero 第二段依序揭露英文 kicker、「厄倫蒂兒／好氣質」、兩行介紹、CTA 與人物視覺；正常播放從 3.15 秒後銜接，略過片頭時改為 0.08 秒後銜接。
- Hero 主標只做一次性進場，到位後固定不再呼吸，避免文字移動造成卡頓感；人物約 7 秒一循環、最大上移 20px／縮放 1.014，將主要生命感集中在角色。
- Hero 新增 6 個 CSS 星體物件：兩顆星芒、兩顆行星、一個月牙與一組帶衛星的軌道環；依不同延遲進場，定位後以 5.6～9 秒不等的週期、方向與位移持續漂浮。手機隱藏軌道環及一顆次要行星，並調整其餘星體位置與尺寸。
- 內建瀏覽器確認 Hero 初始 opacity 0、進場後 opacity 1，兩次時間取樣的 transform 不同且動畫保持 running；略過後 1.1 秒內標題、文案與人物皆完成顯示，Console error 為 0。
- 下一步由使用者確認主觀節奏、視覺與手機排版；確認後再更新結論、commit 及 push。
